# Contributors

When building this project you can perform linting, formatting and updates to
openapi spec using xtask:

```bash
cargo clippy
cargo fmt
cargo xtask generate-openapi
cargo xtask generate-help > docs/HELP.md
```
